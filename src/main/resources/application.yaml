spring:
  datasource:
        username: macaroni_orders
        password: macaroni_orders
        url: jdbc:mysql://localhost:3306/macaroni_orders
        
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
    
  sql:
    init:
      mode: always
